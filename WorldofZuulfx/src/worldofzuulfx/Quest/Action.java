package worldofzuulfx.Quest;

/**
 * This interface represents a PostAction for a Quest. The interface contains
 * only one method 'execute', that executes a given statement after completing a
 * Quest.
 */
public interface Action {

    public void execute();
}
